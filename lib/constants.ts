import { toObject } from "./utils/toObject";

export const RETURN_VALUE_ERROR_CODES = toObject([
  "TRANSACTION_RAN_OUT_OF_GAS",
  "TRANSACTION_UNDERPRICED",
  "REJECTED_TRANSACTION",
  "CALL_REVERTED",
  "EXECUTION_REVERTED",
  "NONCE_TOO_LOW",
  "INSUFFICIENT_FUNDS_FOR_GAS",
  "MAX_PRIORITY_FEE_PER_GAS_HIGHER_THAN_MAX_FEE_PER_GAS",
  "MAX_FEE_PER_GAS_LESS_THAN_BLOCK_BASE_FEE",
  "UNKNOWN_ERROR",
] as const);

export const ETHERS_ERROR_CODES = toObject([
  "NONCE_EXPIRED",
  "UNPREDICTABLE_GAS_LIMIT",
  "ACTION_REJECTED",
  "CALL_EXCEPTION",
] as const);

export const NESTED_ETHERS_ERROR_CODES = {
  REJECTED_TRANSACTION: 4001,
  REQUIRE_TRANSACTION: -32603,
  ERROR_WHILE_FORMATTING_OUTPUT_FROM_RPC: -32603,
  TRANSACTION_UNDERPRICED: -32000,
};
